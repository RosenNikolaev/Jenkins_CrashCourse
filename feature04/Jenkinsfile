pipeline {
    agent any
    stages {
        stage("Build") {
            steps {          
                sh "pwd"
                sh "ls -la"
                sh "whoami"
                sh "chmod +x mvnw"
                sh "mvn -v" 
            }
        }
    }
}
